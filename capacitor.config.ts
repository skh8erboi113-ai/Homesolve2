import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.equityarc.app',
  appName: 'EquityArc',
  webDir: 'out',
  server: {
    androidScheme: 'https',
    hostname: 'studio-4450623487-72853.web.app'
  }
};

export default config;
